Hi {{ first_name }},

Noticed {{ trigger_context }} at {{ company_name }}{{ ' on ' + vessel_name if vessel_name else '' }}.
From similar operators, we see {{ benchmark_percent }}% face {{ predicted_challenge }} within {{ horizon_days }} days—
often costing around {{ cost_per_day }} per day in detention and delay.

Here’s the good news: we can help you {{ solution_outcome }}.
We currently have {{ talent_count }} USCG-certified {{ role }} available who can start {{ start_window }}.
Quick proof: {{ proof_line }}.

If you want, reply "PREVENT" and I’ll send the shortlist + availability.
(Or book a quick 10-min slot: {{ booking_link }})

— {{ sender_name }}
